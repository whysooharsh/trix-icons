{
  "name": "trix-icons",
  "version": "0.1.2",
  "description": "Source-distribution CLI installer for trix-icons animated React icon components",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "trix-icons": "./bin/trix-icons.js"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm && node scripts/bundle-assets.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trix/registry": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
